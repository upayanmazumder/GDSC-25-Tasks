import headerStyles from "./Header.module.css";

const Header = () => {
    return (
        <header className={headerStyles.header}>
            <h1>Upayan's GDSC Recruitments Submission</h1>
        </header>
    );
}

export default Header;